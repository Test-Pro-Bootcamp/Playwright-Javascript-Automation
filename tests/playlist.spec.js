import { test } from '@playwright/test';

test.describe(
  'Playlist management',
  { tag: '@smoke' },
  () => {
    test('create a new playlist', async ({ page }) => {
      // test logic will be added later
    });
  }
);
